# NOTE: Primary deployment is Vercel (see vercel.json).
# This Netlify config is kept as a fallback / mirror site config.
# Remove if Netlify mirror is no longer used.

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
